# 缺陷复现报告

## 1. 问题概述

代码沙箱系统在处理产生大量输出的代码执行请求时，存在输出截断但状态误报的问题。当用户提交的代码产生的标准输出超过系统配置的输出大小限制时，系统错误地将执行结果标记为"成功完成"，但实际返回的输出是被截断后的不完整数据，且没有向用户报告任何错误信息。

## 2. 环境信息

- 操作系统：Linux
- Go 版本：1.22+
- 项目：CodeSandbox - 在线代码执行沙箱
- 运行参数：默认 MaxOutputSize 为 10MB（可通过配置文件调整为较小值如 512 字节以触发缺陷）

## 3. 复现步骤

1. 进入项目根目录，执行 `go build ./...` 确保编译通过
2. 启动代码沙箱服务
3. 提交一段打印大量标准输出的代码（代码产生的输出量需超过 MaxOutputSize 限制）
4. 等待执行完成，观察执行结果

## 4. 实际结果

- 执行结果状态显示为 `Completed`（成功）
- ExitCode 为 0
- 返回的标准输出是截断后的不完整数据
- 没有任何错误信息提示用户输出被截断

## 5. 期望结果

- 当输出超过缓冲区大小时，应检测到输出截断
- 执行结果应标记为失败或至少警告用户
- 应返回适当的错误信息告知用户输出不完整

## 6. 触发频率

必现（100%）：只要提交的代码产生的标准输出超过 MaxOutputSize 限制，就会稳定触发此缺陷。

## 7. 影响范围

- 用户提交产生大量输出的代码时，系统错误地报告执行成功
- 返回的输出是截断后的不完整数据，用户无法得知数据不完整
- 可能导致用户基于不完整的输出做出错误判断
- 可能导致下游系统处理错误结果，造成数据不一致

## 8. 附加说明

临时规避方法：在提交代码前，确保标准输出量不超过系统配置的 MaxOutputSize 限制。若需要产生大量输出，可分批执行或增大输出缓冲区配置。
