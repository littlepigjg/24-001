# Bug 复现报告

## 问题描述

短链接服务在用户创建短链接后，首次通过重定向接口或查询接口访问该短链接时，服务直接崩溃并返回 500 错误。

## 复现步骤

1. 创建一个短链接
2. 通过短链接 code 调用重定向接口（HandleRedirect）
3. 或直接调用查询接口（Get）获取短链接详情

## 预期结果

- 重定向接口应返回 302 跳转到原始 URL
- 查询接口应返回短链接的完整信息（包含 Code、RawURL、Visits、Custom、Disabled、CreatedAt 等字段）

## 实际结果

- 服务返回 500 Internal Server Error
- 触发运行时 panic，错误信息：`interface conversion: interface {} is int64, not string`

## 影响范围

- 所有已创建的短链接均无法正常访问
- 创建操作本身正常，短链接可成功保存
- 只要涉及读取已创建短链接的操作（重定向、查询），均会触发崩溃

## 补充信息

- 此问题为必现缺陷，首次访问即可稳定复现
- 错误日志指向类型转换异常，表明系统在读取某种数据时尝试进行了不安全的类型断言
- 涉及的数据类型包括：整数型的访问计数（visits）、布尔型的自定义标记（custom/disabled）、时间戳（created_at）等
